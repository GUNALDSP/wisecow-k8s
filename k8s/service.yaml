apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
  namespace: wisecow-app
spec:
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4499
      nodePort: 30080   # NodePort for Minikube
  type: NodePort
